{
  "name": "postgresql_event",
  "version": "1.0.0",
  "description": "PostgresSQL events",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch src/ --exec \"yarn start:dev\" --verbose -e ts",
    "start:dev": "yarn build; node ./build/src/server.js"
  },
  "devDependencies": {
    "@types/node": "^13.13.1",
    "@types/pg": "^7.14.3",
    "nodemon": "^2.0.3",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "pg": "^8.0.2"
  }
}
